"use strict";

const greeting1=function createGreeting(name, age){
const yearOfBirth= 2019-age;
    return `Hi, my name is ${name} and im ${age} years old, I was born in ${yearOfBirth}`;};


console.log(greeting1);